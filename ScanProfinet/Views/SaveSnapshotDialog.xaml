<Window x:Class="ScanProfinet.Views.SaveSnapshotDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Salvar rede" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource Background}" WindowStyle="SingleBorderWindow">
    <StackPanel Margin="20">
        <TextBlock Text="Salvar rede escaneada" Style="{StaticResource H1}"/>
        <TextBlock Style="{StaticResource Subtle}" Margin="0,4,0,16"
                   Text="Dê um nome para poder comparar esta rede no futuro (ex.: RedeCliente1)."/>

        <TextBlock Text="Nome da rede" Style="{StaticResource SectionLabel}"/>
        <TextBox x:Name="NameBox" Margin="0,0,0,14"/>

        <TextBlock Text="Observações (opcional)" Style="{StaticResource SectionLabel}"/>
        <TextBox x:Name="NotesBox" Height="70" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalContentAlignment="Top" Margin="0,0,0,18"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Style="{StaticResource BtnGhost}" Width="100" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Salvar" Style="{StaticResource BtnSuccess}" Width="120" Click="Save_Click" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
